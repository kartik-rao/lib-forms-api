F6h3t#8n
NEW REGISTRATION
    create cognito user
    create user -> Mutation addUser
    create account -> Mutation addAccount
    association Lambda (cognito)
        setUserTenant -> "accountId"
        setUserGroup -> "Admin"
    create plan -> Mutation addPlan (with accountId)
    choose plan [Free / Plan] -> Mutation updateAccount (planId)

INVITE USER
    Lambda
        cognito createUser with group and tenant
        cognito sendInvite